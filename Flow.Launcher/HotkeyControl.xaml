<UserControl
    x:Class="Flow.Launcher.HotkeyControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:input="clr-namespace:System.Windows.Input;assembly=PresentationCore"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="CustomHotkeyControl"
    mc:Ignorable="d">
    <Grid>
        <Button x:Name="HotkeyBtn" Content="{Binding Path=HotkeyTarget, ElementName=CustomHotkeyControl}">
            <Button.Style>
                <Style TargetType="{x:Type Button}">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{DynamicResource ButtonInsideBorder}"
                                    BorderThickness="1"
                                    CornerRadius="5">
                                    <ItemsControl x:Name="BtnList" ItemsSource="{Binding items}">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel Orientation="Horizontal" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border
                                                    Margin="6"
                                                    Padding="10,5,10,5"
                                                    Background="{DynamicResource SystemAccentColorLight2Brush}"
                                                    BorderThickness="1"
                                                    CornerRadius="5">
                                                    <ContentPresenter
                                                        x:Name="ContentPresenter"
                                                        Focusable="False"
                                                        RecognizesAccessKey="True"
                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemContainerStyle>
                                            <Style TargetType="ContentPresenter" />
                                        </ItemsControl.ItemContainerStyle>
                                    </ItemsControl>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="FontSize" Value="13" />
                    <Setter Property="FontWeight" Value="Bold" />
                    <Setter Property="Background" Value="{DynamicResource Color12B}" />
                    <Setter Property="Foreground" Value="{DynamicResource Color01B}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource ButtonOutBorder}" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource CustomContextHover}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ButtonMousePressed}" />
                            <Setter Property="BorderThickness" Value="{DynamicResource PressedCustomButtonOutBorderThickness}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource ButtonMousePressedInsideBorder}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource ButtonInsideMouseOverBorder}" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>


        <Popup
            x:Name="popup"
            AllowDrop="True"
            AllowsTransparency="True"
            IsOpen="{Binding IsKeyboardFocused, ElementName=tbHotkey, Mode=OneWay}"
            Placement="Top"
            PlacementTarget="{Binding ElementName=tbHotkey}"
            PopupAnimation="Fade"
            StaysOpen="True"
            VerticalOffset="-5">
            <Border
                Width="140"
                Height="30"
                Background="{DynamicResource Color01B}"
                BorderBrush="{DynamicResource Color21B}"
                BorderThickness="1"
                CornerRadius="4">
                <TextBlock
                    x:Name="tbMsg"
                    Margin="0,0,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource Color05B}"
                    Text="{DynamicResource flowlauncherPressHotkey}"
                    Visibility="Visible" />
            </Border>
        </Popup>

        <TextBox
            x:Name="tbHotkey"
            Margin="0,0,18,0"
            VerticalContentAlignment="Center"
            input:InputMethod.IsInputMethodEnabled="False"
            GotFocus="tbHotkey_GotFocus"
            LostFocus="tbHotkey_LostFocus"
            PreviewKeyDown="TbHotkey_OnPreviewKeyDown"
            TabIndex="100"
            Visibility="Collapsed" />
    </Grid>
</UserControl>