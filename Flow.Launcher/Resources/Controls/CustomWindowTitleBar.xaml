<UserControl
    x:Class="Flow.Launcher.Resources.Controls.CustomWindowTitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Flow.Launcher.Resources.Controls"
    Height="32"
    SnapsToDevicePixels="True"
    UseLayoutRounding="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Image
            x:Name="IconImage"
            Grid.Column="0"
            Width="16"
            Height="16"
            Margin="10 4 4 4"
            RenderOptions.BitmapScalingMode="HighQuality"
            Source="{Binding IconSource, RelativeSource={RelativeSource AncestorType={x:Type local:CustomWindowTitleBar}}}" />
        <TextBlock
            x:Name="TitleTextBlock"
            Grid.Column="1"
            Margin="4 0 0 0"
            VerticalAlignment="Center"
            FontSize="12"
            Foreground="{DynamicResource Color05B}"
            Text="{Binding Title, RelativeSource={RelativeSource AncestorType={x:Type local:CustomWindowTitleBar}}}" />

        <Button
            x:Name="MinimizeButton"
            AutomationProperties.Name="Minimize"
            Grid.Column="2"
            Click="MinimizeButton_OnClick"
            RenderOptions.EdgeMode="Aliased"
            Style="{StaticResource TitleBarButtonStyle}">
            <Path 
                Data="M 18,15 H 28"
                Style="{StaticResource TitleBarButtonPathStyle}" />
        </Button>
        <Button
            x:Name="MaximizeButton"
            AutomationProperties.Name="Maximize"
            Grid.Column="3"
            Click="MaximizeRestoreButton_OnClick"
            Style="{StaticResource TitleBarButtonStyle}">
            <Path 
                Data="M 18.5,10.5 H 27.5 V 19.5 H 18.5 Z"
                Style="{StaticResource TitleBarButtonPathStyle}" />
        </Button>
        <Button
            x:Name="RestoreButton"
            AutomationProperties.Name="Restore"
            Grid.Column="3"
            Click="MaximizeRestoreButton_OnClick"
            Style="{StaticResource TitleBarButtonStyle}"
            Visibility="Collapsed">
            <Path 
                Data="M 18.5,12.5 H 25.5 V 19.5 H 18.5 Z M 20.5,12.5 V 10.5 H 27.5 V 17.5 H 25.5"
                Style="{StaticResource TitleBarButtonPathStyle}" />
        </Button>
        <Button
            x:Name="CloseButton"
            AutomationProperties.Name="Close"
            Grid.Column="4"
            Click="CloseButton_OnClick"
            Style="{StaticResource TitleBarCloseButtonStyle}">
            <Path 
                Data="M 18,11 27,20 M 18,20 27,11"
                Style="{StaticResource TitleBarButtonPathStyle}" />
        </Button>
    </Grid>
</UserControl>
