This directory is intentionally tracked for local agent-related project context.

OpenCode itself uses the root `opencode.json` project config file; `.gemini` is intentionally not kept in this repository.
